const enum Enum {
  /**
   *
   */
  MS = '-ms-',
  MOZ = '-moz-',
  WEBKIT = '-webkit-',

  /**
   *
   */
  COMMEN = 'comment',
  RULESET = 'ruleset',
  DECLARATION = 'declaration',

  /**
   *
   */
  PAGE = '@page',
  MEDI = '@media',
  IMPOR = '@import',
  CHARSE = '@charset',
  VIEWPOR = '@viewport',
  SUPPORT = '@supports',
  DOCUMENT = '@document',
  NAMESPACE = '@namespace',
  KEYFRAMES = '@keyframes',
  FONT_FACE = '@font-face',
  COUNTER_STYLE = '@counter-style',
  FONT_FEATURE_VALUES = '@font-features-values'
}
